syntax = "proto3";//不知道

package isearch;//名字随便起的样子

service Hello{//服务名及方法名
    rpc apiBridge(req) returns (res){}
}

message req{//定义接收包
    string url=1;
    string req=2;
    string headers=3;
    string method=4;    
}

message res{//定义返回包
    string result =1;
}